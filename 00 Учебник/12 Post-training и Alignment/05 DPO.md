---
title: DPO — непосредственная оптимизация предпочтений
type: textbook-chapter
status: canonical
last_updated: 2026-07-20
aliases:
  - DPO
prerequisites:
  - "[[02 Areas/ML & DL/00 Учебник/12 Post-training и Alignment/04 Policy gradient и PPO для LLM]]"
next:
  - "[[02 Areas/ML & DL/00 Учебник/12 Post-training и Alignment/06 RLVR и verifiers]]"
primary_sources:
  - https://arxiv.org/abs/2305.18290
---

# DPO: непосредственная оптимизация предпочтений

> [!abstract] После этой главы
> Вы сможете вывести DPO из KL-регуляризованной задачи RLHF, объяснить, почему
> из формулы исчезает нормировочная константа, и вручную рассчитать функцию
> потерь для пары ответов. Мы также разберём границы метода: стандартный DPO не
> порождает новые ответы во время обучения и по-прежнему нуждается в опорной
> модели.

## 1. Какую часть RLHF упрощает DPO

Предположим, для запроса $x$ есть два ответа:

$$
y_w \succ y_l,
$$

где $y_w$ предпочёл человек или оценивающая модель, а $y_l$ был отвергнут.
Классический процесс RLHF состоит из следующих этапов:

1. обучить модель награды на таких сравнениях;
2. породить новые ответы текущей моделью;
3. оценить их моделью награды;
4. обновить языковую модель с помощью PPO, ограничивая её отклонение от опорной.

DPO начинается с вопроса: если награда нужна лишь для определения оптимальной
стратегии, нельзя ли выразить вероятность предпочтения сразу через саму
языковую модель? При определённых предпосылках это возможно. В результате
получается функция потерь, похожая на бинарную классификацию и обучающая модель
непосредственно на заранее собранных парах ответов.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/dpo/dpo-pipeline.png]]

*Слева показан обычный RLHF с отдельной моделью
награды, справа — обучение языковой модели непосредственно на предпочтениях.
Процесс становится проще, однако качество данных и предположение о скрытой
численной полезности ответов остаются столь же важными. Rafailov et al.,
[Direct Preference Optimization, Figure 1, с. 2](https://arxiv.org/pdf/2305.18290#page=2).*

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/post-training-courses/dpo-offline-vs-online.png]]

*Практическое различие между DPO и обучением с подкреплением: DPO многократно
использует фиксированные пары, тогда как PPO порождает новые ответы текущей
стратегией. Поэтому «нет отдельной модели награды» не означает, что два метода
видят одинаковые данные или исследуют одинаковые области ответов. Источник:
Nathan Lambert,
[RLHF & Post-Training, лекция 6, слайд 48](https://rlhfbook.com/teach/course/lec6-chap8-dpo/#/47).*

## 2. Вероятность целого ответа

Для авторегрессионной языковой модели

$$
\log\pi_\theta(y\mid x)
=\sum_{t=1}^{T_y}
\log\pi_\theta(y_t\mid x,y_{<t}).
$$

DPO сравнивает **логарифмы вероятностей полных ответов**, а не вероятность
последнего токена и не среднюю уверенность модели в отдельных словах. При
реализации из логитов размера `[B,L,V]` выбирают логарифмы вероятностей целевых
токенов, исключают маской запрос и заполнитель, а затем суммируют значения на
позициях ответа.

Из-за суммирования результат зависит от длины: логарифм вероятности длинной
последовательности обычно более отрицателен. Сравнение с опорной моделью частично
компенсирует этот эффект, но смещение данных в пользу длинных ответов всё равно
нужно проверять отдельно.

## 3. Модель предпочтений как отправная точка

Bradley–Terry model задаёт:

$$
P(y_w\succ y_l\mid x)
=\sigma(r(x,y_w)-r(x,y_l)),
$$

где $\sigma(z)=1/(1+e^{-z})$. Обычная функция потерь модели награды:

$$
\mathcal L_{RM}
=-\mathbb E\log\sigma(r_\phi(x,y_w)-r_\phi(x,y_l)).
$$

Из предпочтений можно восстановить лишь разность наград. Если к оценкам обоих
ответов на один запрос прибавить $C(x)$, вероятность предпочтения не изменится.
Именно благодаря этому свойству далее сократится неизвестная нормировочная
константа.

## 4. Задача RLHF с ограничением по KL-дивергенции

Для каждого запроса рассмотрим следующую задачу оптимизации стратегии:

$$
\max_\pi
\mathbb E_{y\sim\pi(\cdot\mid x)}[r(x,y)]
-\beta D_{KL}(\pi(\cdot\mid x)\Vert\pi_{ref}(\cdot\mid x)).
$$

$\pi_{ref}$ — обычно модель после SFT, а $\beta>0$ определяет цену отклонения от
неё. Раскроем математическое ожидание:

$$
J(\pi)=\sum_y\pi(y\mid x)
\left[r(x,y)-\beta\log\frac{\pi(y\mid x)}{\pi_{ref}(y\mid x)}\right]
$$

при ограничении $\sum_y\pi(y\mid x)=1$. Добавим множитель Лагранжа $\lambda$:

$$
\mathcal F=J(\pi)+\lambda\left(\sum_y\pi(y\mid x)-1\right).
$$

Производная по $\pi(y\mid x)$:

$$
r(x,y)-\beta\left(
\log\frac{\pi(y\mid x)}{\pi_{ref}(y\mid x)}+1
\right)+\lambda=0.
$$

После перегруппировки и нормировки получаем распределение Больцмана:

$$
\pi_r(y\mid x)=
\frac{1}{Z(x)}\pi_{ref}(y\mid x)
\exp\left(\frac{r(x,y)}{\beta}\right),
$$

$$
Z(x)=\sum_y\pi_{ref}(y\mid x)
\exp\left(\frac{r(x,y)}{\beta}\right).
$$

Это ключевая связь в выводе DPO: оптимальная стратегия получается из опорной
модели экспоненциальным перевзвешиванием ответов согласно их награде.

## 5. Выражаем скрытую награду через языковую модель

Берём логарифм:

$$
\log\pi_r(y\mid x)
=\log\pi_{ref}(y\mid x)+\frac{r(x,y)}{\beta}-\log Z(x).
$$

Отсюда

$$
r(x,y)=
\beta\log\frac{\pi_r(y\mid x)}{\pi_{ref}(y\mid x)}
+\beta\log Z(x).
$$

Для пары одного prompt:

$$
\begin{aligned}
r(x,y_w)-r(x,y_l)
=\beta\bigg[
&\log\frac{\pi_r(y_w\mid x)}{\pi_{ref}(y_w\mid x)}\\
-&\log\frac{\pi_r(y_l\mid x)}{\pi_{ref}(y_l\mid x)}
\bigg].
\end{aligned}
$$

$\beta\log Z(x)$ одинаков для обоих ответов и **сокращается**. Поэтому DPO не
приходится вычислять огромную сумму по всем возможным текстам: попарное
сравнение устраняет нормировочную константу.

Подставим параметризованную стратегию $\pi_\theta$ в вероятность Брэдли — Терри:

$$
\boxed{
\mathcal L_{DPO}
=-\mathbb E_{(x,y_w,y_l)}
\log\sigma\left(
\beta\left[
\log\frac{\pi_\theta(y_w\mid x)}{\pi_{ref}(y_w\mid x)}
-\log\frac{\pi_\theta(y_l\mid x)}{\pi_{ref}(y_l\mid x)}
\right]
\right)}.
$$

## 6. Что именно сравнивает функция потерь DPO

Введём изменение логарифма вероятности относительно опорной модели:

$$
\Delta_\theta(x,y)=
\log\pi_\theta(y\mid x)-\log\pi_{ref}(y\mid x).
$$

Тогда аргумент сигмоиды в DPO равен

$$
z=\beta[\Delta_\theta(x,y_w)-\Delta_\theta(x,y_l)].
$$

Функция потерь требует не просто $\pi_\theta(y_w)>\pi_\theta(y_l)$, а чтобы
текущая модель предпочитала выбранный ответ **сильнее, чем опорная модель**.

### Ручной пример

Пусть логарифмы вероятностей полных ответов равны:

| | выбранный $y_w$ | отвергнутый $y_l$ |
|---|---:|---:|
| текущая модель $\log\pi_\theta$ | −3.0 | −4.0 |
| опорная модель $\log\pi_{ref}$ | −3.4 | −3.6 |

Тогда

$$
\Delta_w=-3.0-(-3.4)=0.4,
\qquad
\Delta_l=-4.0-(-3.6)=-0.4.
$$

При $\beta=0.5$:

$$
z=0.5(0.4-(-0.4))=0.4,
$$

$$
P_\theta(y_w\succ y_l)=\sigma(0.4)\approx0.599,
$$

$$
L=-\log0.599\approx0.513.
$$

Если текущая модель повысит логарифмы вероятностей обоих ответов относительно
опорной на одинаковые `+0.4`, разность останется нулевой: DPO не обнаружит
усиления предпочтения выбранного ответа.

### Как действует градиент

Неявная награда, соответствующая текущей модели:

$$
\hat r_\theta(x,y)=
\beta\log\frac{\pi_\theta(y\mid x)}{\pi_{ref}(y\mid x)}.
$$

Градиент функции потерь DPO:

$$
\nabla_\theta L_{DPO}
=-\beta\,\mathbb E\left[
\sigma(\hat r_l-\hat r_w)
\left(
\nabla\log\pi_\theta(y_w\mid x)
-\nabla\log\pi_\theta(y_l\mid x)
\right)
\right].
$$

Такое обновление

- повышает логарифм правдоподобия выбранного ответа;
- понижает логарифм правдоподобия отвергнутого;
- сильнее взвешивает пары, которые текущая неявная награда ранжирует неправильно.

В ручном примере вес равен $\sigma(-0.4)\approx0.401$. Если модель перепутает
ответы и получит $z=-2$, вес возрастёт до $\sigma(2)\approx0.881$. Это важная
часть алгоритма: в статье показано, что наивный вариант без такого взвешивания
может вырождаться.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/post-training-courses/dpo-gradient.png]]

*Градиент DPO в разложенном виде: коэффициент слева усиливает неправильно
упорядоченные пары, а разность справа одновременно повышает вероятность
выбранного ответа и понижает вероятность отклонённого. Источник: Nathan
Lambert,
[RLHF & Post-Training, лекция 6, слайд 39](https://rlhfbook.com/teach/course/lec6-chap8-dpo/#/38),
вывод по Rafailov et al., 2023.*

## 7. Что означает $\beta$

В исходной задаче с подкреплением $\beta$ определяет силу ограничения по
KL-дивергенции. В DPO он масштабирует аргумент сигмоиды. Отсюда следует:

- при малом $\beta$ одна и та же разность отношений вероятностей слабее насыщает
  сигмоиду, поэтому градиенты дольше остаются заметными;
- при большом $\beta$ даже умеренная разность быстрее насыщает классификатор;
- численные значения $\beta$ нельзя сравнивать, не учитывая способ суммирования,
  длину ответов, набор данных и нормировку в реализации.

$\beta$ нельзя называть скоростью обучения: скорость обучения меняет величину
шага оптимизатора, тогда как $\beta$ меняет саму статистическую задачу.

## 8. Данные и порядок вычислений

Одна запись набора данных имеет вид:

```json
{
  "prompt": "Объясни TCP и UDP...",
  "chosen": "TCP обеспечивает...",
  "rejected": "UDP всегда быстрее..."
}
```

После применения шаблона диалога формируются две последовательности с общим
запросом.

```text
prompt + chosen   → policy logits [B,Lw,V] → gather → sum response → π_chosen [B]
prompt + rejected → policy logits [B,Ll,V] → gather → sum response → π_reject [B]
                                           │
те же sequences → frozen reference → ref_chosen [B], ref_reject [B]
                                           │
chosen_logratio  = π_chosen - ref_chosen
reject_logratio  = π_reject - ref_reject
logit             = β(chosen_logratio - reject_logratio)
loss              = -logsigmoid(logit)
```

На практике выбранные и отвергнутые ответы часто объединяют в один пакет для
текущей модели и ещё один для опорной. Так уменьшаются затраты на заполнители и
обмен данными между устройствами.

```python
def sequence_logp(logits, labels, response_mask):
    token_logp = log_softmax(logits[:, :-1], -1).gather(
        -1, labels[:, 1:, None]
    ).squeeze(-1)
    return (token_logp * response_mask[:, 1:]).sum(-1)

pi_w = sequence_logp(policy(chosen), chosen_ids, chosen_mask)
pi_l = sequence_logp(policy(rejected), rejected_ids, rejected_mask)

with no_grad():
    ref_w = sequence_logp(reference(chosen), chosen_ids, chosen_mask)
    ref_l = sequence_logp(reference(rejected), rejected_ids, rejected_mask)

logit = beta * ((pi_w - ref_w) - (pi_l - ref_l))
loss = -logsigmoid(logit).mean()
```

При реализации особенно важно следующее:

- токены запроса не входят в логарифм вероятности ответа;
- служебный префикс роли ассистента должен обрабатываться одинаково во всех последовательностях;
- заполнение и обрезка не должны незаметно менять целевой ответ;
- для неизменного набора данных значения опорной модели можно вычислить заранее;
- сумма и среднее логарифмов вероятностей токенов задают разные задачи; в
  исходной статье используется сумма, то есть логарифм вероятности всей последовательности.

## 9. Откуда берётся опорная модель

В стандартной схеме обучения:

1. базовая предобученная модель проходит SFT;
2. полученная контрольная точка копируется;
3. одна копия становится обучаемой DPO-моделью;
4. вторая замораживается и служит опорной.

Если пары предпочтений были созданы другой моделью, возникает сдвиг
распределения. В статье о DPO при отсутствии исходной SFT-модели сначала
дообучают модель на выбранных ответах $(x,y_w)$, а полученную версию используют
одновременно для инициализации и как опорную.

Опорная модель участвует и в функции потерь, и в определении неявной награды.
Фраза «DPO без опорной модели» относится к более поздним вариантам метода, но
не к стандартному DPO.

## 10. DPO и PPO: общая постановка, разные способы обучения

| | PPO RLHF | DPO |
|---|---|---|
| входной сигнал | награда за сгенерированный ответ | готовые пары выбранных и отвергнутых ответов |
| генерация во время обучения | да | нет |
| отдельная модель награды | да | нет |
| опорная модель | да | да |
| функция ценности | обычно да | нет |
| исследование новых ответов | да | нет |
| основной риск | использование слабостей награды и нестабильность | неполнота, шум и переобучение на парах |

Функция потерь DPO выводится из той же задачи максимизации награды с ограничением
по KL-дивергенции. Однако практические алгоритмы не становятся полностью
эквивалентными. Заранее собранные данные не меняются вслед за моделью,
предпосылки модели Брэдли — Терри могут нарушаться, а PPO способен исследовать
ответы, которых не было в исходных парах.

## 11. Какие данные нужны

Качество DPO определяется не только правильностью метки:

- запросы должны соответствовать условиям, в которых модель будет применяться;
- ответы-кандидаты должны быть достаточно сильными и при этом различимыми;
- очевидные пары быстро перестают давать полезный градиент;
- почти равноценные ответы нельзя насильно превращать в уверенную бинарную метку;
- необходимо измерять смещение, связанное с позицией, длиной и стилем ответа;
- пары, полученные от значительно отличающейся модели, создают несовпадение распределений;
- повторные сравнения для одного запроса нельзя бездумно считать независимыми;
- попадание тестовых ответов в обучение через оценщика остаётся утечкой данных.

Вместе с каждой парой полезно хранить её происхождение, версию породившей
модели, параметры декодирования, автора оценки, уверенность и критерии
сравнения. Простая запись из двух ответов без этих полей плохо подходит для
последующего аудита.

## 12. Характерные ошибки и ограничения

### Вероятность отвергнутого ответа падает быстрее, чем растёт вероятность выбранного

Точность на парах может расти только за счёт резкого снижения вероятности
отвергнутых ответов, хотя вероятность выбранных ответов тоже падает. Поэтому
оценки выбранных и отвергнутых ответов, разность между ними и исходные логарифмы
вероятностей следует наблюдать раздельно.

### Смещение в пользу длины

Люди и автоматические оценщики могут предпочитать более подробные ответы. Кроме
того, сумма логарифмов вероятностей зависит от числа токенов. Нужны оценивание
по группам длины, пары сопоставимой длины и проверка качества после
контролируемого сокращения или расширения ответа.

### Шумные метки и нетранзитивность

Модель Брэдли — Терри предполагает существование скрытой скалярной полезности.
Реальные предпочтения могут зависеть от контекста и образовывать циклы: A яснее
B, B точнее C, а C безопаснее A. Одно число не обязано согласовать все критерии.

### Насыщение сигмоиды

Когда разность оценок велика, вес градиента приближается к нулю и пара почти
перестаёт влиять на обучение. Это ожидаемое поведение классификатора, а не
доказательство абсолютного качества модели.

### Переобучение на неизменном наборе пар

Модель может запомнить поверхностные признаки, разделяющие выбранные и
отвергнутые ответы. Поэтому обязательна проверка на отложенных запросах и новых
ответах, отсутствовавших в обучении.

### Несовпадение шаблонов диалога

Если опорная и обучаемая модели видят разные служебные токены ролей, отношение
вероятностей теряет смысл. Токенизатор, шаблон диалога, правила обрезки и
обработка токена конца должны совпадать.

### Оценивание повторяет смещения обучающего оценщика

Высокая доля побед по мнению того же оценщика не является независимой
проверкой. Нужны человеческая оценка выборки, другая модель-оценщик, проверяемые
ответы для задач с однозначным результатом и сравнения с контролем стиля.

## 13. Диагностика обучения

Минимальный dashboard:

- DPO loss и preference accuracy;
- mean $\hat r_w$, $\hat r_l$ и margin;
- raw current/reference log-probs отдельно;
- chosen/rejected response lengths;
- KL current–reference на свежих generations;
- held-out pair accuracy;
- independent win-rate и task correctness;
- diversity, verbosity, refusals и format compliance;
- stratification по source, domain, language, length и annotator confidence.

> [!warning]
> Training pair accuracy может приблизиться к 100%, а модель стать хуже для
> пользователей. Это всего лишь способность воспроизвести ordering fixed pairs.

## 14. Варианты — после понимания standard DPO

- **IPO** меняет statistical treatment preferences и стремится избежать части
  overfitting behavior DPO.
- **ORPO** совмещает SFT-like likelihood и odds-ratio preference objective без
  отдельной reference model.
- **SimPO** использует length-normalized reward и reference-free margin.
- **KTO** работает с desirable/undesirable examples без явных pairs.
- **Online DPO** обновляет preference data samples текущей policy.

Их не стоит изучать как список аббревиатур. Для каждого надо задать четыре
вопроса: какой data schema, какой implicit preference model, есть ли reference,
и откуда берутся candidates во время training.

## Практика и первоисточники

### Задания

1. Воспроизведите ручной пример при $\beta=0.1$, $1$ и $5$. Постройте sigmoid
   weight как функцию margin.
2. Докажите сокращение $Z(x)$; объясните, почему нельзя сравнить два ответа от
   разных prompts тем же шагом.
3. Для пары посчитайте DPO loss, если current и reference совпадают. Какова
   preference probability и почему gradient всё равно ненулевой?
4. Реализуйте `sequence_logp` и тесты: padding не влияет на сумму; prompt mask
   равен нулю; EOS учитывается ровно один раз.
5. Создайте synthetic dataset, где chosen всегда длиннее. Обучите маленькую
   model/classifier и измерьте, стал ли length shortcut.
6. Возьмите один chosen и три rejected: фактическая ошибка, плохой стиль и
   нарушение формата. Проверьте, переносится ли learned preference между типами.
7. Сравните DPO и SFT только на chosen: какие gradients получает rejected и
   почему результаты различаются?

### Проверка понимания

Вопросы к этой главе вынесены в
[[02 Areas/ML & DL/04 Вопросы/Вопросы по LLM#Post-training и alignment|«Вопросы по LLM»]].

### Литература и реализации

#### Первоисточник

- Rafailov et al., [Direct Preference Optimization: Your Language Model is
  Secretly a Reward Model](https://arxiv.org/abs/2305.18290). Особенно Figure 1,
  Equations 1–7, gradient после Eq. 7, Figure 2 и Appendix A.

#### Объяснения и код

- Stanford CS224N, Nathan Lambert, *Life after DPO* — локальные slides:
  `raw/courses/Stanford CS224N/slides/cs224n-spr2024-lecture15-life-after-dpo-lambert.pdf`.
- Nathan Lambert, [RLHF Book](https://rlhfbook.com/) — preference data,
  reward models и direct alignment algorithms.
- Hugging Face, [TRL DPO Trainer](https://huggingface.co/docs/trl/dpo_trainer).
- Hugging Face, [Alignment Handbook](https://github.com/huggingface/alignment-handbook) — воспроизводимые SFT/DPO recipes.

> [!summary] Главное
> DPO исключает отдельное обучение модели награды и цикл PPO: стратегия сразу
> обучается на парах выбранных и отклонённых ответов. Такой переход возможен
> благодаря аналитической связи между наградой и оптимальной стратегией в
> задаче с ограничением по KL-дивергенции. Нормировочная константа сокращается
> только при сравнении ответов на один и тот же запрос. Метод проще в запуске,
> но по-прежнему зависит от опорной модели, предпосылок модели Брэдли—Терри и
> полноты заранее собранных данных предпочтений.
