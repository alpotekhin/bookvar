---
title: MHA, MQA и GQA
type: textbook-chapter
status: canonical
last_updated: 2026-07-20
previous: "[[02 Areas/ML & DL/00 Учебник/07 Анатомия современной LLM/01 LLaMA как базовая архитектура]]"
next: "[[02 Areas/ML & DL/00 Учебник/08 Эффективный Attention и длинный контекст/02 MLA и сжатие KV-cache]]"
primary_sources:
  - https://arxiv.org/abs/1911.02150
  - https://aclanthology.org/2023.emnlp-main.298/
  - https://github.com/stanford-cs336/spring2025-lectures/blob/main/lecture_10.py
  - https://arxiv.org/abs/2307.09288
---

# MHA, MQA и GQA: сколько комплектов ключей и значений хранить

Ускорение авторегрессионной генерации начинается с простого наблюдения. Когда
модель предсказывает очередной токен, ключи и значения предыдущих позиций уже не
изменятся. Их можно вычислить один раз и сохранить. Благодаря этому для нового
токена выполняется один проход через блоки модели, а не повторная обработка всего
префикса. Такая память называется **KV-кэшем**.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/source-first-35-36/cs336-prefill-decode.png]]

*Префилл обрабатывает исходную последовательность параллельно, а декодирование
добавляет по одному токену и на каждом шаге обращается к накопленному KV-кэшу.
Источник: Stanford CS336, [Lecture 1: Language Modeling from
Scratch](https://github.com/stanford-cs336/spring2025-lectures/blob/main/lecture_01.py),
рисунок `prefill-decode.png`.*

Экономия вычислений оборачивается расходом памяти. Каждый запрос имеет собственный
кэш; кэш растёт с длиной контекста, числом слоёв и размером пакета. MHA, MQA и GQA
различаются не формулой внимания, а числом независимых комплектов ключей и
значений, которые создаёт и хранит каждый слой.

## Почему декодирование упирается в чтение памяти

Пусть префикс содержит $S$ токенов. Для очередной позиции нужен запрос
$q_{S+1}$, который сравнивается со всеми сохранёнными ключами:

$$
\operatorname{Attn}(q_{S+1},K_{1:S},V_{1:S})=
\operatorname{softmax}\!\left(
\frac{q_{S+1}K_{1:S}^{\top}}{\sqrt{d_h}}
\right)V_{1:S}.
$$

В режиме префилла длины запросов и ключей велики, поэтому крупные матричные
умножения хорошо загружают ускоритель. При декодировании длина запроса равна
единице, но все $S$ ключей и значений всё равно приходится читать из HBM. В
выводе Stanford CS336 вычислительная интенсивность слоя внимания без учёта
второстепенных операций равна

$$
I=\frac{ST}{S+T},
$$

где $T$ — число одновременно обрабатываемых новых позиций. Для префилла $T=S$ и
$I=S/2$; для одного шага декодирования $T=1$ и $I=S/(S+1)<1$. Увеличение пакета
не устраняет этот предел: у каждой последовательности свой KV-кэш. Поэтому
сокращение кэша уменьшает не только занятую память, но и объём данных, который
нужно переносить на каждом шаге.

## Размер KV-кэша выводится из форм тензоров

Обозначим через $B$ размер пакета, $L$ число слоёв, $S$ число сохранённых токенов,
$n_{kv}$ число голов ключей и значений, $d_h$ размерность головы, а через $b$
число байтов на элемент. Если размерности ключа и значения одинаковы, то

$$
\boxed{\text{KV bytes}=2BLSn_{kv}d_hb}.
$$

Множитель $2$ отвечает за ключи и значения. В формуле нет числа голов запросов
$n_q$: запросы прошлых токенов не сохраняются.

Рассмотрим слой с $n_q=32$, $d_h=128$ и модель из $L=32$ слоёв. Для одной
последовательности длиной $8192$ в BF16 ($b=2$) MHA использует $n_{kv}=32$:

$$
2\cdot1\cdot32\cdot8192\cdot32\cdot128\cdot2
=4\,294\,967\,296\ \text{bytes}=4\ \text{GiB}.
$$

Это только KV-кэш — без параметров модели, временных тензоров и служебной памяти.
При $n_{kv}=8$ тот же кэш занимает $1$ GiB, а при $n_{kv}=1$ — $128$ MiB. Так
возникает главная ось компромисса между MHA, GQA и MQA.

## Три способа организовать головы

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/gqa/mha-gqa-mqa.png]]

*В MHA каждой голове запроса соответствуют собственные K/V; в GQA запросы
разделены на группы; в MQA все запросы используют один комплект K/V. Источник:
Joshua Ainslie et al., [GQA: Training Generalized Multi-Query Transformer Models
from Multi-Head Checkpoints](https://aclanthology.org/2023.emnlp-main.298/),
Figure 2.*

### MHA: свой комплект K/V у каждой головы запроса

В обычном многоголовом внимании

$$
n_{kv}=n_q=h.
$$

Для головы $i$ вычисляются отдельные проекции

$$
q_i=XW_Q^{(i)},\qquad k_i=XW_K^{(i)},\qquad v_i=XW_V^{(i)}.
$$

Каждая голова может выучить собственное пространство поиска и собственное
представление переносимого содержимого. Цена такой свободы — хранение $h$
комплектов K/V на каждом слое.

### MQA: один комплект K/V для всех запросов

В Multi-Query Attention число голов запросов сохраняется, но ключ и значение
становятся общими:

$$
n_q=h,\qquad n_{kv}=1.
$$

Запросы по-прежнему различны, поэтому головы получают разные веса внимания и
разные выходы. Общим является только представление адресов и содержимого, к
которому они обращаются. Ноам Шазир предложил MQA именно для уменьшения объёма
чтения KV-кэша при пошаговой генерации; возможная плата — ухудшение качества из-за
слишком сильного совместного использования K/V.

### GQA: один комплект K/V на группу запросов

Grouped-Query Attention занимает промежуточное положение:

$$
1<n_{kv}<n_q,\qquad n_q=n_{kv}r,
$$

где $r$ — число голов запросов в группе. При равномерном разбиении голова запроса
$i$ использует комплект

$$
g(i)=\left\lfloor\frac{i}{r}\right\rfloor,
\qquad i=0,\ldots,n_q-1.
$$

Например, при $n_q=8$ и $n_{kv}=2$ запросы 0–3 используют $K_0,V_0$, а запросы
4–7 — $K_1,V_1$. При $n_q=32$ и $n_{kv}=8$ каждый комплект K/V обслуживает
четыре головы запроса, поэтому кэш в четыре раза меньше, чем у MHA.

Крайние случаи той же формулы дают MQA при $n_{kv}=1$ и MHA при
$n_{kv}=n_q$. Именно поэтому GQA — не отдельная несовместимая операция, а
континуум способов совместно использовать K/V.

## Формы тензоров и логическое повторение K/V

Пусть

$$
X\in\mathbb R^{B\times T\times d},\quad n_q=32,\quad
n_{kv}=8,\quad d_h=128.
$$

После проекций и перестановки осей получаются

$$
Q\in\mathbb R^{B\times32\times T\times128},
$$

$$
K,V\in\mathbb R^{B\times8\times T\times128}.
$$

Для записи внимания в привычном виде K и V можно **логически** повторить по
четыре раза, получив вид размерности
$B\times32\times T\times128$. Физически создавать четыре копии нельзя: тогда
исчезнет экономия пропускной способности. Оптимизированное ядро направляет четыре
головы запросов к одной голове K/V либо использует представление без копирования.

Та же разница видна в матрицах проекций:

$$
W_Q\in\mathbb R^{d\times n_qd_h},\qquad
W_K,W_V\in\mathbb R^{d\times n_{kv}d_h}.
$$

Поэтому GQA сокращает не только кэш, но и число параметров и вычисления двух
проекций. Выходная проекция сохраняет ширину $n_qd_h$.

## От готовой MHA-модели к GQA

Статья GQA изучала преобразование уже обученного MHA-чекпойнта. Проекции ключей и
значений внутри будущей группы усредняются:

$$
\bar W_K^{(g)}=\frac{1}{|G_g|}\sum_{i\in G_g}W_K^{(i)},
\qquad
\bar W_V^{(g)}=\frac{1}{|G_g|}\sum_{i\in G_g}W_V^{(i)}.
$$

После преобразования модель дополнительно обучают. Авторы назвали этот этап
**uptraining** и исследовали бюджет около 5% исходного предобучения: усреднение
само по себе не обязано согласовать пространства, выученные разными головами.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/gqa/uptrain-recycling.png]]

*Проекции K/V исходной MHA-модели объединяются, после чего обучение продолжается
с новым устройством голов. Источник: Joshua Ainslie et al., [GQA: Training
Generalized Multi-Query Transformer Models from Multi-Head
Checkpoints](https://aclanthology.org/2023.emnlp-main.298/), Figure 1.*

## Что показывают эксперименты GQA

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/source-first-35-36/gqa-quality-latency-table.png]]

*В опытах на T5-XXL модель GQA-8 после дообучения почти восстановила среднее
качество MHA-XXL при времени вывода, близком к MQA. Сравнение имеет смысл только
в рамках данной аппаратуры, задач и длины последовательностей. Источник: Ainslie
et al., [GQA](https://aclanthology.org/2023.emnlp-main.298/), Table 1; фрагмент
подготовлен в Stanford CS336, Lecture 10.*

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/source-first-35-36/gqa-groups-speed.png]]

*Время на пример растёт по мере приближения числа групп к MHA; одна группа
совпадает с MQA. Источник: Ainslie et al.,
[GQA](https://aclanthology.org/2023.emnlp-main.298/), Figure 6; версия рисунка из
Stanford CS336, Lecture 10.*

Эти результаты не устанавливают универсально лучшего числа групп. Больше
$n_{kv}$ даёт более выразительные K/V, но увеличивает память и обмен данными;
меньше $n_{kv}$ освобождает место для большего пакета и повышает потенциальную
пропускную способность. Итог зависит от архитектуры, задачи, длины контекста,
точности кэша, реализации ядра и схемы параллелизма.

## Как распознать вариант внимания в модели

В конфигурациях Llama и совместимых моделей обычно достаточно двух полей:

```json
{
  "num_attention_heads": 32,
  "num_key_value_heads": 8
}
```

Первое задаёт $n_q$, второе — $n_{kv}$. Их равенство означает MHA, единица во
втором поле — MQA, промежуточное значение — GQA. LLaMA 1 использовала MHA;
Llama 2 применила GQA в модели 70B; в опубликованных размерах Llama 3 GQA уже
стала штатным решением. PaLM — один из известных примеров MQA.

Уменьшение KV-кэша не делает внимание линейным по длине префилла и не заменяет
FlashAttention. FlashAttention меняет порядок вычислений и сокращает обмен с
памятью внутри операции внимания; GQA уменьшает сами тензоры K/V. Эти методы
совместимы.

## Контрольные выводы

- в KV-кэше хранятся ключи и значения прошлых токенов, а не запросы и не матрица
  весов внимания;
- MQA и GQA не уменьшают число голов запросов;
- размер кэша определяется $n_{kv}$ и растёт линейно с пакетом, слоями и длиной
  контекста;
- логическое повторение K/V допустимо, их физическое копирование уничтожает часть
  выигрыша;
- уменьшение кэша создаёт возможность ускорения, но не гарантирует
  пропорционального уменьшения задержки всей модели.

## Источники и продолжение

- [Stanford CS336, Lecture 10: Inference](https://github.com/stanford-cs336/spring2025-lectures/blob/main/lecture_10.py)
  — последовательность «арифметическая интенсивность → KV-кэш → GQA → MLA» и
  численный анализ памяти.
- [Noam Shazeer — Fast Transformer Decoding: One Write-Head is All You
  Need](https://arxiv.org/abs/1911.02150) — исходная постановка MQA.
- [Joshua Ainslie et al. — GQA](https://aclanthology.org/2023.emnlp-main.298/)
  — группировка, преобразование MHA-чекпойнта и эксперименты с дообучением.
- [Llama 2: Open Foundation and Fine-Tuned Chat
  Models](https://arxiv.org/abs/2307.09288) — применение GQA в Llama 2 70B.
- [PaLM: Scaling Language Modeling with Pathways](https://arxiv.org/abs/2204.02311)
  — применение MQA.
- [[02 Areas/ML & DL/01 Справочник/Attention/MHA]],
  [[02 Areas/ML & DL/01 Справочник/Attention/MQA]],
  [[02 Areas/ML & DL/01 Справочник/Attention/GQA]].

**Дальше:** [[02 Areas/ML & DL/00 Учебник/08 Эффективный Attention и длинный контекст/02 MLA и сжатие KV-cache|MLA: как хранить не готовые K/V, а их компактный общий источник.]]
