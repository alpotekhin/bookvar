---
title: "После полного внимания: SSM, Mamba, linear attention, TTT и гибриды"
type: textbook-chapter
status: canonical
last_updated: 2026-09-01
last_verified: 2026-09-01
previous: "[[02 Areas/ML & DL/00 Учебник/09 Dense FFN и Mixture of Experts/02 Mixture of Experts — routing, capacity и serving]]"
next: "[[02 Areas/ML & DL/00 Учебник/11 Pre-training и Scaling/41 Сбор, очистка и смеси данных]]"
primary_sources:
  - https://arxiv.org/abs/2312.00752
  - https://arxiv.org/abs/2405.21060
  - https://arxiv.org/abs/2305.13048
  - https://arxiv.org/abs/2503.14456
  - https://arxiv.org/abs/2307.08621
  - https://arxiv.org/abs/2403.19887
  - https://arxiv.org/abs/2312.04927
  - https://arxiv.org/abs/2412.06464
  - https://arxiv.org/abs/2407.04620
  - https://arxiv.org/abs/2512.23675
  - https://arxiv.org/abs/2402.01032
  - https://arxiv.org/abs/2406.07887
  - https://arxiv.org/abs/2603.15569
  - https://platform.minimax.io/docs/guides/text-m2-full-attention
course_sources:
  - https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_05_Sequence_Models.pdf
  - https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_06_Seq2seq.pdf
  - https://www.cs.cmu.edu/~mgormley/courses/10423/slides/lecture21-ssm.pdf
  - https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_14_Advanced_Architectures.pdf
---

# После полного внимания: SSM, Mamba, linear attention, TTT и гибриды

Представим контекст как телефонную книгу. В начале документа записано несколько
тысяч пар «имя — номер», а вопрос «какой номер у Алисы?» появляется только в
конце. Пока модель читает книгу, она ещё не знает, какая запись понадобится.
Значит, архитектура должна либо сохранить доступ ко всем записям, либо сжать их
в ограниченную память и надеяться, что нужная деталь переживёт сжатие.

Именно это различие связывает RNN, Transformer, Mamba, RWKV, линейное внимание и
Test-Time Training. Они отличаются не лозунгом «линейная сложность», а
**контрактом памяти**: что остаётся после обработки токена и какую операцию над
прошлым может выполнить будущий запрос.

> [!abstract] Что нужно понять
> В Transformer ключи и значения прошлых токенов сохраняются по отдельности,
> поэтому новый запрос может обратиться к любой позиции по её содержанию.
> Рекуррентная модель вместо этого поддерживает состояние фиксированного размера:
> память при генерации не растёт вместе с контекстом, но вся история должна быть
> сжата в это состояние. Mamba учится управлять записью и чтением такого
> состояния, RWKV и RetNet допускают разные формы вычисления при обучении и
> генерации, linear attention хранит матрицу ассоциаций, TTT превращает
> состояние в обучаемую модель, а гибриды сохраняют редкое внимание для точного
> поиска по контексту.

## Как мы пришли к Transformer — и почему снова обсуждаем recurrence

Современные альтернативы вниманию легче понять не как коллекцию новых названий,
а как продолжение старого спора о памяти последовательной модели. RNN, LSTM,
Seq2Seq с attention и Transformer последовательно меняли ответ на один вопрос:
**в каком виде хранить прошлое, чтобы будущий токен мог получить нужную
информацию?**

### RNN: один скрытый вектор проходит через всю последовательность

В обычной рекуррентной сети один и тот же блок применяется на каждом шаге:

$$
h_t=\tanh(W_xx_t+W_hh_{t-1}+b),\qquad
y_t=W_yh_t.
$$

Состояние $h_t$ одновременно является результатом обработки текущего токена и
единственным каналом, через который всё прошлое влияет на будущее. Развёрнутая
во времени диаграмма выглядит как глубокая сеть с общими весами: чем дальше
полезный сигнал находится от текущего шага, тем через большее число одинаковых
переходов он должен пройти.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/stanford-cs230-rnn-unrolled.png]]

*RNN в свёрнутой и развёрнутой записи. Источник: Shervine Amidi, Afshine Amidi,
[Stanford CS230 — Recurrent Neural Networks cheatsheet](https://stanford.edu/~shervine/teaching/cs-230/cheatsheet-recurrent-neural-networks/).*

При обучении методом backpropagation through time градиент многократно
умножается на матрицу перехода и производные нелинейности. Если соответствующие
множители обычно меньше единицы, сигнал экспоненциально затухает; если больше —
взрывается. Gradient clipping ограничивает взрыв, но не создаёт долговременную
память. Поэтому ранняя RNN могла моделировать локальную последовательность, но
с трудом сохраняла конкретную деталь на сотни шагов.

### LSTM: отдельный путь памяти и управляемые вентили

LSTM добавляет состояние ячейки $c_t$ и три явных решения: что забыть из
$c_{t-1}$, что записать из текущего входа и какую часть показать наружу. В
упрощённой записи:

$$
c_t=f_t\odot c_{t-1}+i_t\odot \tilde c_t,
\qquad h_t=o_t\odot \tanh(c_t).
$$

Суммирующий путь через $c_t$ позволяет градиенту проходить без обязательного
многократного сжатия одной и той же нелинейностью. Это не устраняет ограничение
фиксированного состояния, но превращает память из побочного эффекта RNN в
управляемый механизм записи и забывания.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/stanford-cs230-lstm.png]]

*Ячейка LSTM: forget, update и output gates управляют отдельной линией памяти.
Источник: [Stanford CS230 — Recurrent Neural Networks cheatsheet](https://stanford.edu/~shervine/teaching/cs-230/cheatsheet-recurrent-neural-networks/).*

### Seq2Seq: вся исходная фраза не помещается в один context vector

Классический encoder–decoder использовал одну RNN для чтения исходной
последовательности и другую — для генерации перевода. Последнее состояние
энкодера становилось фиксированным context vector. Это хорошо работало на
коротких фразах, но создавало информационное бутылочное горлышко: прежде чем
начать перевод, энкодер должен был сжать все имена, числа, синтаксические связи и
порядок слов в один вектор.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/berkeley-seq2seq-bottleneck.png]]

*Последнее состояние encoder должно представить всю исходную последовательность.
Источник: Berkeley CS288, [Lecture 6 — Sequence-to-Sequence Models](https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_06_Seq2seq.pdf), slide 51.*

Bahdanau attention изменил место принятия решения. Вместо одного неизменного
context vector decoder на каждом шаге вычисляет оценки соответствия своему
текущему состоянию всем состояниям encoder, нормирует их и собирает новый
контекст:

$$
e_{t,j}=a(s_{t-1},h_j),\qquad
\alpha_{t,j}=\operatorname{softmax}_j(e_{t,j}),\qquad
c_t=\sum_j\alpha_{t,j}h_j.
$$

Теперь модель сначала узнаёт, какое слово собирается генерировать, и только
потом выбирает, какую часть входа прочитать. Для телефонной книги это решающий
сдвиг: полезность записи может определиться после того, как весь архив уже
построен.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/berkeley-bahdanau-attention.png]]

*Пошаговое чтение состояний encoder механизмом attention. Источник: Berkeley
CS288, [Lecture 6 — Sequence-to-Sequence Models](https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_06_Seq2seq.pdf), slide 65; схема следует Bahdanau et al.*

### Transformer: адресуемая память становится основным mixer

Transformer убирает последовательную recurrence из основного пути и разрешает
каждой позиции напрямую смешивать сведения из других позиций. В decoder-only
языковой модели causal mask оставляет доступ только к прошлому, но внутри этого
прошлого каждый новый query может заново выбрать релевантные key–value записи.
Параллелизм обучения резко улучшается, а фиксированное состояние заменяется
архивом, размер которого растёт вместе с контекстом.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/berkeley-transformer-language-model.png]]

*Decoder-only Transformer: attention смешивает позиции, FFN смешивает каналы,
residual stream переносит представление через глубину. Источник: Berkeley CS288,
[Lecture 5 — Sequence Models](https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_05_Sequence_Models.pdf).*

Получился исторический маятник. RNN и LSTM экономно переносили сжатое состояние,
но теряли произвольный доступ к деталям. Attention и Transformer вернули такой
доступ ценой растущей памяти и дорогих взаимодействий. SSM, Mamba, RWKV,
линейное внимание и TTT снова исследуют recurrence, но уже с параллельными
алгоритмами обучения, hardware-aware kernels и более выразительными правилами
обновления состояния.

## Два способа помнить прошлое

### Внимание: архив с доступом по содержанию

Для нового запроса $q_t$:

$$
y_t=\sum_{j\le t}
\operatorname{softmax}_j(q_t^\top k_j)v_j.
$$

Прошлые ключи $k_j$ и значения $v_j$ хранятся явно. Запрос текущего токена
определяет, какие записи прочитать и с какими весами их сложить.

### Рекуррентность: постоянно обновляемое резюме

$$
h_t=f(h_{t-1},x_t),\qquad y_t=g(h_t).
$$

После обработки токена $t$ всё прошлое доступно только через $h_t$. Память при
генерации не растёт с длиной контекста, но деталь, не сохранившуюся в состоянии,
уже нельзя извлечь повторным обращением к исходной позиции.

```text
внимание:       x₁ x₂ x₃ ... xₜ  → хранить отдельные K/V
рекуррентность: h₀ → h₁ → h₂ → ... → hₜ  → хранить hₜ
```

В этом состоит основное различие. Линейная вычислительная сложность сама по себе
не говорит, насколько хорошо модель умеет точно извлекать сведения по их
содержанию.

## Почему недостаточно сказать «Transformer квадратичен»

При обработке уже известного входа — **prefill** — полное внимание сравнивает
$O(T^2)$ пар позиций. FlashAttention уменьшает обмен с HBM и не материализует
полную матрицу оценок, но не меняет множество взаимодействий: каждый запрос всё
ещё может зависеть от большого числа ключей.

При **decode** новый токен добавляется по одному. KV-cache избавляет от повторного
вычисления старых ключей и значений, но сам архив растёт как $O(T)$. Каждый новый
запрос читает всё большую историю. Поэтому длинная генерация упирается не только
в FLOPs, но и в ёмкость и пропускную способность памяти.

Рекуррентный слой обещает другой режим:

- prefill выполняет линейный объём работы сканированием или блоками;
- decode обновляет состояние фиксированного размера;
- число уже обработанных токенов не увеличивает state одного запроса.

Однако $O(T)$ не означает «быстрее на любом GPU». Плохо реализованный scan может
проиграть зрелому attention kernel на коротком входе. Отдельное состояние всё
равно требуется каждой активной последовательности. Поэтому архитектурную
асимптотику проверяют отдельно на prefill, decode, batch и конкретном ускорителе.

## От динамической системы к SSM

Линейная модель пространства состояний в непрерывном времени задаётся
уравнениями:

$$
\frac{dh(t)}{dt}=Ah(t)+Bx(t),
$$

$$
y(t)=Ch(t)+Dx(t).
$$

После дискретизации времени:

$$
h_t=\bar Ah_{t-1}+\bar Bx_t,
$$

$$
y_t=Ch_t+Dx_t.
$$

Матрица $A$ определяет, как сохраняется и затухает предыдущее состояние; $B$
записывает в него вход, $C$ преобразует состояние в выход, а $D$ создаёт прямой
путь от входа к выходу.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/mamba/ssm-three-views.png]]

*Одна и та же линейная стационарная SSM может быть записана как рекуррентное
обновление, свёртка или структурированное сканирование. Эти формы математически
связаны, но по-разному удобны для последовательной генерации и параллельного
обучения.*

## Откуда берётся параллельная форма вычисления

Разворачивая recurrence:

$$
h_t=\bar A^t h_0+
\sum_{j=1}^{t}\bar A^{t-j}\bar Bx_j.
$$

При $h_0=0$:

$$
y_t=\sum_{j=1}^{t}C\bar A^{t-j}\bar Bx_j.
$$

Ядро:

$$
K_\tau=C\bar A^\tau\bar B
$$

зависит только от расстояния $\tau$, но не от содержания токенов. Поэтому выходы
для всей последовательности можно получить свёрткой. Рекуррентная запись удобна
для пошаговой генерации, а свёрточная — для параллельной обработки обучающего
примера.

Однако одно и то же ядро для любых входов плохо подходит к задаче «сохрани
значение после специального маркера и игнорируй последующий шум». Модели нужен
способ решать, какую информацию записывать.

## Mamba: избирательная модель пространства состояний

В Mamba параметры $\Delta$, $B$ и $C$ становятся функциями текущего токена:

$$
\Delta_t=s_\Delta(x_t),\qquad
B_t=s_B(x_t),\qquad
C_t=s_C(x_t).
$$

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/mamba-selective-copying.png]]

*В обычном copying одна и та же динамика применяется ко всем символам; в
selective copying модель должна распознать маркеры, записать только выбранные
токены и сохранить их через отвлекающий участок. Источник: Gu & Dao,
[Mamba](https://arxiv.org/abs/2312.00752), Figure 2.*

Теперь модель может:

- сильнее обновить состояние на важном токене;
- почти не менять его на проходном фрагменте;
- читать разные компоненты состояния в зависимости от текущего входа.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/mamba/selective-ssm.png]]

*Ключевая иллюстрация статьи о Mamba: механизм отбора делает обновление SSM
зависимым от входа. Цвет показывает, что разные токены по-разному записываются
в состояние и считываются из него.*

### Сквозной пример

```text
Код доступа: 7319. ... много текста ... Какой код доступа?
```

Идеальное поведение:

```text
«Код доступа:» → открыть запись
7319            → сильно обновить состояние
остальной текст → сохранить нужную компоненту
«Какой код?»    → прочитать её
```

Механизм внимания может найти `7319`, непосредственно сопоставив запрос с
ключами прошлых позиций. Селективная SSM должна вовремя записать число в
состояние и не затереть его последующим текстом. Это два принципиально разных
пути к одному ответу.

## Почему Mamba уже нельзя вычислить одной обычной свёрткой

Когда $B_t$, $C_t$ и $\Delta_t$ зависят от входа, ядро перестаёт быть
стационарным. Одну заранее вычисленную свёртку уже нельзя применить ко всем
примерам.

Поэтому Mamba использует селективное сканирование, спроектированное с учётом
устройства ускорителя:

- объём работы остаётся линейным по длине последовательности;
- ассоциативная структура обновлений позволяет выполнять часть работы
  параллельно;
- расширенное внутреннее состояние по возможности не записывается многократно
  в медленную внешнюю память ускорителя;
- объединение вычислительных ядер и повторное вычисление промежуточных величин
  становятся частью алгоритма.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/mamba-selective-scan-course.png]]

*После появления input-dependent параметров стационарное свёрточное ядро нельзя
подготовить один раз. Mamba использует hardware-aware selective scan. Источник:
CMU 10-423/623/723, [State Space Models and Hybrid Models](https://www.cs.cmu.edu/~mgormley/courses/10423/slides/lecture21-ssm.pdf), slide 26.*

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/mamba/mamba-block.png]]

*Блок Mamba: входное преобразование разветвляется, короткая свёртка обрабатывает
локальные закономерности, селективная SSM — долговременное состояние, а
управляющий вентиль формирует итоговый выход.*

## Вычислительная стоимость и память

У каузального Transformer:

- при обработке входного контекста матрица оценок внимания содержит $O(T^2)$
  пар позиций;
- при генерации KV-кэш растёт как $O(T)$;
- каждый новый токен читает весь накопленный кэш соответствующего слоя.

У рекуррентной SSM:

- объём работы линейно растёт с длиной последовательности;
- состояние при генерации не растёт с $T$;
- каждый новый токен выполняет обновление фиксированного размера.

Асимптотика не определяет время выполнения целиком. Расширение состояния,
сканирование, короткая свёртка и линейные преобразования могут сделать Mamba на
коротких последовательностях медленнее хорошо оптимизированного внимания.

## RWKV: attention-подобное смешивание как recurrence

Название RWKV раскрывается как Receptance Weighted Key Value. Блок сочетает:

- смешивание информации между позициями;
- смешивание каналов внутри представления токена;
- параллельную форму вычисления для обучения;
- рекуррентную форму для пошаговой генерации.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/rwkv-figure3-hq.png]]

*Архитектура RWKV из исходной статьи. Существенна не формула «Transformer без
внимания», а возможность вычислять один и тот же блок параллельно по
последовательности при обучении и через состояние при генерации.*

Упрощённая идея weighted memory:

$$
s_t=\lambda\odot s_{t-1}+\phi(k_t,v_t),
$$

$$
y_t=r_t\odot \operatorname{read}(s_t),
$$

где receptance $r_t$ играет роль gate. Реальные версии RWKV различаются, поэтому
эта формула — intuition, а не checkpoint specification.

Начиная с RWKV-5 состояние удобно рассматривать не как один вектор, а как
матрицу накопленных key–value ассоциаций. RWKV-7 сохраняет этот общий принцип,
но делает переход динамическим: текущий токен предсказывает направления записи,
стирания и скорость обновления. Это сближает механизм с generalized delta rule:
память не только пассивно затухает, а направленно исправляет уже записанные
ассоциации.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/rwkv7-state-update.png]]

*Один head RWKV-7 обновляет matrix state с зависимыми от входа направлениями и
вентилями. Источник: Peng et al., [RWKV-7 «Goose»](https://arxiv.org/abs/2503.14456),
Figure 2.*

Важно не смешивать архитектуру и релиз модели. RWKV-7 — опубликованное правило
памяти; конкретные контрольные точки G1 дополнительно зависят от данных и
post-training. Специализированная экосистема RWKV умеет запускать эти модели, но
это не доказывает такую же совместимость с mainstream quantization, prefix
caching и distributed serving, как у Llama-подобных Transformer.

## RetNet: три эквивалентных формы retention

Механизм retention в RetNet можно вычислять тремя способами:

1. параллельно — при обучении;
2. рекуррентно — при генерации по одному токену;
3. рекуррентно по блокам — при работе с длинными последовательностями.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/retnet-figure3-hq.png]]

*Двойственная форма RetNet: один механизм допускает параллельную и рекуррентную
запись.*

Блочная форма особенно полезна как инженерный компромисс:

```text
внутри блока → параллельные матричные операции
между блоками → рекуррентное состояние
```

Таким образом, выбор не сводится к противопоставлению полностью параллельного
Transformer и полностью последовательной RNN.

## Mamba-2 и Mamba-3: развитие механизма

Mamba-2 и теория State Space Duality показывают более глубокую связь между
структурированными моделями пространства состояний и некоторыми матричными
операциями, похожими на внимание. SSD разбивает длинную последовательность на
блоки: внутри блока применяются эффективные матричные операции, а между блоками
переносится агрегированное состояние.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/mamba2-block-figure.png]]

*Mamba-2 перестраивает блок вокруг SSD: prompt обрабатывается эффективной
параллельной формой, а при decode переносится рекуррентное состояние. Источник:
Dao & Gu, [Transformers are SSMs](https://arxiv.org/abs/2405.21060), Figure 5.*

Фраза «Transformers are SSMs» не означает, что произвольный softmax Transformer
и Mamba имеют одинаковую память. Двойственность относится к определённому классу
структурированных матриц. Полное внимание по-прежнему хранит отдельные K/V, а
SSD переносит сжатое состояние.

Для учебной траектории важно различать:

- Mamba-1 вводит зависимый от входа отбор и эффективное селективное сканирование;
- Mamba-2 реорганизует блок на основе двойственности моделей пространства
  состояний;
- Mamba-3 улучшает дискретизацию, вводит более выразительную динамику, включая
  комплексное состояние, и MIMO-режим для нескольких каналов состояния без
  пропорционального роста стоимости decode.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/mamba3-figure2.png]]

*Сравнение блоков Mamba-2 и Mamba-3. Источник: Lahoti et al.,
[Mamba-3, Figure 2](https://arxiv.org/abs/2603.15569). Результаты статьи получены
на масштабе до 1.5B: это сильное исследовательское улучшение механизма, но не
доказательство production-зрелости frontier-LLM.*

Актуальный официальный указатель:
[state-spaces/mamba](https://github.com/state-spaces/mamba). На 2026 год он уже
содержит код Mamba-3. Версии, границы экспериментов и зрелость runtime вынесены в
[[02 Areas/ML & DL/02 Атлас моделей/Семейства/Mamba|карточку семейства Mamba]].

## Linear attention как матричное состояние

Линейное внимание приходит к фиксированному состоянию другим путём. Если
сходство запроса и ключа представить через признаки
$\phi(q)^\top\phi(k)$, ассоциативность матричного умножения позволяет сначала
свернуть ключи и значения:

$$
\phi(Q)\bigl(\phi(K)^\top V\bigr).
$$

В каузальном режиме поддерживается матрица ассоциаций

$$
S_t=S_{t-1}+\phi(k_t)v_t^\top,
$$

а чтение выполняется как

$$
y_t=\phi(q_t)^\top S_t.
$$

Для нормированного варианта отдельно накапливается

$$
z_t=z_{t-1}+\phi(k_t),\qquad
y_t=\frac{\phi(q_t)^\top S_t}
{\phi(q_t)^\top z_t+\varepsilon}.
$$

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/cmu-linear-attention.png]]

*Ассоциативность превращает linear attention в recurrence с матричным
состоянием: сначала обновляется $S_t$, затем текущий query читает его. Источник:
CMU 10-423/623/723, [State Space Models and Hybrid Models](https://www.cs.cmu.edu/~mgormley/courses/10423/slides/lecture21-ssm.pdf), slide 30.*

Это похоже на ассоциативную память: ключ задаёт направление записи, значение
помещается в матрицу, а будущий запрос считывает комбинацию записей. Prefill
вычисляется блоками, decode — рекуррентно.

> [!warning] Linear attention — не просто «убрали softmax»
> Практические варианты различаются feature map, нормировкой, gates, decay,
> локальной свёрткой и правилом обновления. Перестановка скобок объясняет
> источник линейности, но не является спецификацией любой современной модели.

## Delta rule: не только записать, но и исправить

Простое накопление outer products только добавляет ассоциации. Если один ключ
раньше означал одно значение, а затем получил другое, старые и новые записи
интерферируют. Delta rule сначала вычисляет текущее предсказание памяти:

$$
\hat v_t=S_{t-1}k_t,
$$

а затем записывает ошибку:

$$
S_t=S_{t-1}+\beta_t(v_t-\hat v_t)k_t^\top.
$$

Если память уже выдаёт правильное значение, обновление мало. Если ассоциация
устарела, правило направленно её исправляет. Gate дополнительно управляет
забыванием:

$$
S_t=\alpha_tS_{t-1}
+\beta_t(v_t-S_{t-1}k_t)k_t^\top.
$$

Gated DeltaNet объединяет коррекцию с адаптивным стиранием и параллельным
алгоритмом обучения.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/gated-deltanet-figure1.png]]

*Два гибридных варианта и устройство Gated DeltaNet. Источник: Yang, Kautz &
Hatamizadeh, [Gated Delta Networks, Figure 1](https://arxiv.org/abs/2412.06464),
ICLR 2025.*

Mamba и Gated DeltaNet решают похожую системную задачу — переносят компактное
состояние, — но не являются одним механизмом. Mamba исходит из управляемой
динамической системы; DeltaNet — из линейной ассоциативной памяти и online
correction.

## Test-Time Training: состоянием становится модель

Словосочетание *test-time training* употребляется для разных процессов. Их
нужно развести до обсуждения формул:

1. **Test-time adaptation** подстраивает модель к сдвигу распределения тестовых
   данных, например по неразмеченному потоку изображений.
2. **Reasoning test-time compute** оставляет веса неизменными, но тратит больше
   шагов генерации, ветвлений или проверок на одно решение.
3. **TTT sequence layer** использует обновляемые параметры маленькой внутренней
   модели как скрытое состояние последовательности.

В этой главе TTT означает третий вариант. Его цель — увеличить выразительность
памяти слоя, а не «дать LLM подольше подумать» и не адаптировать один глобальный
checkpoint ко всем будущим запросам.

У обычной RNN состояние — вектор или матрица. В TTT-layer состоянием становится
маленькая модель с параметрами $W_t$. Читая токен, она выполняет шаг
самообучения:

$$
W_t=W_{t-1}-\eta\nabla_W
\ell\bigl(f(x_t;W_{t-1}),\text{ self-supervised target}_t\bigr),
$$

а затем использует обновлённую модель для выдачи представления:

$$
y_t=f(q_t;W_t).
$$

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/ttt-figure3.png]]

*RNN хранит вектор, self-attention — список K/V, а TTT — параметры внутренней
модели. Источник: Sun et al.,
[Learning to (Learn at Test Time), Figure 3](https://arxiv.org/abs/2407.04620).*

TTT-Linear использует линейную внутреннюю модель, TTT-MLP — небольшой MLP. Чем
выразительнее learner, тем больше потенциальная ёмкость памяти, но тем дороже
обновлять его на каждом токене. Внешнее обучение является meta-learning: оно
подбирает начальные параметры и самосупервизируемую задачу так, чтобы online
updates полезно впитывали текущий контекст.

TTT-layer не означает, что вся большая LLM дообучается на пользовательском
запросе. Изменяется локальное состояние слоя, спроектированное как fast weights.
Это также не reasoning test-time compute: модель не генерирует дополнительные
шаги рассуждения, а меняет память о прочитанном.

## TTT-E2E: тот же термин, другой механизм

End-to-End TTT рассматривает длинный контекст как continual learning. Базовая
архитектура остаётся обычным Transformer со sliding-window attention. Во время
чтения документа часть параметров продолжает обучаться на next-token prediction,
а инициализация meta-learned так, чтобы эти обновления помогали на последующей
части того же документа.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/ttt-e2e-figure3.png]]

*Градиент от следующего фрагмента обновляет часть sliding-window модели.
Источник: Tandon et al., [End-to-End Test-Time Training,
Figure 3](https://arxiv.org/abs/2512.23675).*

| Подход | Что является памятью | Базовая архитектура |
|---|---|---|
| TTT-layer | параметры маленькой модели внутри sequence layer | внешний residual/MLP stack |
| TTT-E2E | изменяемая часть параметров основной модели | sliding-window Transformer |

Изменяемые веса на каждый запрос требуют изоляции, snapshot, rollback и reset;
разные update trajectories плохо сочетаются с обычным continuous batching.
Поэтому TTT — важная исследовательская линия памяти, но пока не стандартный
production serving path.

## Почему гибриды возвращают attention

Чистая SSM экономно поддерживает сжатое состояние, а внимание позволяет напрямую
обращаться к отдельным позициям по содержанию. Гибридная модель чередует эти
механизмы:

```text
Mamba → Mamba → Attention → Mamba → Mamba → Attention → ...
```

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/cmu-hybrid-models.png]]

*Гибриды отличаются не наличием модного блока, а расписанием sequence mixers:
где именно стоит attention, где переносится recurrent state и как часто доступно
точное адресуемое чтение. Источник: CMU 10-423/623/723,
[State Space Models and Hybrid Models](https://www.cs.cmu.edu/~mgormley/courses/10423/slides/lecture21-ssm.pdf), slide 38.*

### Jamba

Jamba объединяет три независимо настраиваемые идеи:

- слои обычного внимания Transformer;
- слои Mamba;
- MoE в части слоёв.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/jamba-figure1-hq.png]]

*Архитектура Jamba: тип переноса информации по последовательности, наличие MoE и
соотношение слоёв являются независимыми параметрами. Одного слова «Mamba» для её
описания недостаточно.*

Слои внимания дают прямой путь извлечения сведений из прошлых позиций, слои
Mamba уменьшают среднюю стоимость длинного контекста, а MoE увеличивает ёмкость
модели без пропорционального роста вычислений на токен.

### Три паттерна гибридизации

**Чередование слоёв** помещает разные mixers на разной глубине. Jamba —
классический пример: несколько Mamba-слоёв, затем attention, затем снова Mamba.
Память состоит из фиксированного recurrent state и растущего KV только для
attention-слоёв.

**Повторяющееся расписание** фиксирует ритм, например три state-layer и один
attention-layer. Qwen3-Next использует Gated DeltaNet в большинстве слоёв;
Nemotron 3 — Mamba-2 и редкие global-attention layers. Расписание определяет не
только FLOPs и KV-cache, но и частоту доступного точного чтения.

**Параллельные ветви** запускают два mixers внутри одного блока. Falcon-H1
вычисляет attention и Mamba-2 параллельно и объединяет результаты. Адресуемый и
сжатый пути доступны на каждой глубине, зато ядра и баланс tensor parallelism
становятся сложнее.

MoE — отдельная ось. Он меняет FFN и число активных параметров на токен, но сам
по себе не определяет, как хранится история. Jamba, Qwen3-Next и Nemotron могут
быть одновременно гибридными по памяти и разреженными по экспертам; эти проценты
нельзя складывать.

### Falcon-H1: параллельные пути в одном mixer

Falcon-H1 не ждёт следующего редкого attention-layer. В каждом гибридном mixer
ветвь Mamba-2 переносит сжатое состояние, а ветвь attention выполняет адресуемое
чтение на той же глубине представления; затем их выходы объединяются. Такой
дизайн уменьшает риск, что важная деталь исчезнет до следующей точки attention,
но усложняет распределение вычислений и требует быстрых kernels для обеих
ветвей.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/falcon-h1-architecture.webp]]

*Параллельный hybrid mixer Falcon-H1. Источник: Technology Innovation Institute,
[Falcon-H1 technical report and release materials](https://huggingface.co/tiiuae/Falcon-H1-34B-Base).*

### Qwen3-Next: большинство слоёв — Gated DeltaNet

Qwen3-Next использует повторяющийся ритм: несколько Gated DeltaNet mixers и один
gated attention mixer. DeltaNet дешёво обновляет matrix state, а периодическое
attention оставляет прямой путь к отдельным позициям. Одновременно модель может
быть MoE; разреженность экспертов отвечает за стоимость FFN, а расписание
mixers — за способ хранения истории.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/berkeley-qwen3-next-gdn.png]]

*Qwen3-Next 80B-A3B: чередование Gated DeltaNet и gated attention. Источник:
Berkeley CS288, [Lecture 14 — Advanced Architectures](https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_14_Advanced_Architectures.pdf), slide 38.*

### DeepSeek: давление на full attention без рекуррентной SSM

DeepSeek V2/V3/R1 показывает другую стратегию. Multi-head Latent Attention
сжимает представление ключей и значений, а MoE уменьшает число активных FFN
параметров, но sequence mixer остаётся attention. Прошлые позиции по-прежнему
адресуются запросом; изменяется размер архива, а не его принцип.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/deepseek-v3-architecture.png]]

*DeepSeek-V3 остаётся decoder-only Transformer с MLA и MoE. Источник: DeepSeek-AI,
[DeepSeek-V3 Technical Report](https://arxiv.org/abs/2412.19437), Figure 2.*

### MiniMax M2: отрицательный результат масштабирования

Архитектурный выбор нельзя завершать небольшим controlled benchmark. Команда
MiniMax сообщила, что ранние гибриды Lightning Attention и full attention
выглядели конкурентно на MMLU, BBH, MATH и LongBench, но при масштабировании
обнаружили дефицит в сложных multi-hop reasoning задачах. Дополнительно проявились
чувствительность состояния к низкой точности, менее зрелые prefix caching и
speculative decoding.

Это vendor evidence, а не универсальное доказательство превосходства full
attention. Но это ценный контрпример к аргументу «линейная асимптотика и равный
малый benchmark уже гарантируют лучший production design». В результате M2 был
выпущен с полным attention, а efficient attention остался исследовательской
линией. Источник: MiniMax,
[Why Did MiniMax M2 End Up as a Full Attention Model?](https://platform.minimax.io/docs/guides/text-m2-full-attention).

| Семейство | Основной mixer | Где остаётся attention | Паттерн |
|---|---|---|---|
| Jamba/Jamba2 | Mamba | редкие отдельные слои | interleave/schedule |
| Falcon-H1 | Mamba-2 + attention | параллельная ветвь mixer | parallel |
| Nemotron-H/3 | Mamba-2 | редкие GQA/global layers | schedule |
| Qwen3-Next | Gated DeltaNet | примерно каждый четвёртый mixer | schedule |
| Kimi Linear | Kimi Delta Attention | периодические MLA layers | schedule |
| DeepSeek V3/R1 | MLA attention | во всех sequence layers | не SSM-гибрид |

Конкретные релизы, training recipe и границы доказательств находятся в атласе:
[[02 Areas/ML & DL/02 Атлас моделей/Семейства/Jamba|Jamba]],
[[02 Areas/ML & DL/02 Атлас моделей/Семейства/Falcon|Falcon]],
[[02 Areas/ML & DL/02 Атлас моделей/Семейства/Nemotron|Nemotron]],
[[02 Areas/ML & DL/02 Атлас моделей/Семейства/Qwen|Qwen]] и
[[02 Areas/ML & DL/02 Атлас моделей/Семейства/Kimi|Kimi]].

### Что нужно указывать для гибридной модели

```yaml
attention_layer_ratio:
attention_type:
ssm_type:
state_size:
local_convolution:
moe_layer_ratio:
experts_per_token:
cache:
  kv_per_attention_layer:
  recurrent_state_per_ssm_layer:
```

Две модели, обе названные гибридными, могут иметь совершенно разный расход
памяти. Поэтому недостаточно указать только классы использованных блоков — важно
знать их частоту и параметры состояния.

## Честное сравнение памяти

| Тип модели | Постоянная память при генерации |
|---|---|
| Transformer с полным вниманием | KV всех слоёв × длина контекста |
| Transformer с GQA | уменьшенный KV × длина контекста |
| чистая рекуррентная модель или SSM | состояние фиксированного размера во всех слоях |
| гибрид | KV слоёв внимания × контекст + фиксированное состояние SSM |

В гибридной модели KV-кэш продолжает расти, но только в слоях внимания. Экономия
имеет количественный характер: гибрид уменьшает кэш, а не устраняет его.

## Где recurrent compression проигрывает

Phonebook и multi-query associative recall проверяют случай, где контекст
содержит множество пар ключ–значение, а вопрос появляется только в конце. Модель
не может заранее знать, какую пару попросят. Transformer откладывает выбор до
появления query. Модель с фиксированным состоянием должна вместить все
потенциально полезные пары, и при росте их числа записи начинают интерферировать.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/phonebook-pure-figure3.png]]

*Чистые Mamba/Mamba-2 теряют точность phonebook раньше Transformer. Источник:
Waleffe et al., [An Empirical Study of Mamba-based Language Models,
Phonebook experiments](https://arxiv.org/abs/2406.07887), в продолжение линии
Jelassi et al., [Repeat After Me](https://arxiv.org/abs/2402.01032).*

График не доказывает, что SSM «не понимает длинный текст». Состояние может хорошо
переносить тему, стиль, локальную динамику и повторяющиеся факты. Слабое место
конкретнее: произвольное точное извлечение информационно плотной редкой детали,
полезность которой стала известна только в будущем.

Ограничения сжатого состояния также обнаруживают задачи, где нужно:

- точно скопировать фрагмент;
- найти редкую строку среди большого объёма постороннего текста;
- продолжить ранее встретившийся шаблон;
- совместно извлечь несколько удалённых друг от друга фактов;
- восстановить редкую деталь после длинного отвлекающего фрагмента.

Высокий средний результат на языковом бенчмарке не гарантирует точного
извлечения из длинного контекста. Perplexity почти не замечает один неверный
номер среди тысяч обычных токенов. Проверка должна воспроизводить тот вид памяти,
который нужен приложению.

### Почему редкое внимание помогает

Если добавить небольшую долю attention-слоёв, модель получает отдельные точки
адресуемого чтения. Рекуррентные слои продолжают дёшево поддерживать общую
динамику, а внимание страхует случаи, где нужна исходная запись.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/alternatives-transformer-2026/phonebook-hybrid-figure7.png]]

*Mamba-2-Hybrid значительно дольше сохраняет точность phonebook при контекстах
4K, 16K и 32K. Источник: Waleffe et al.,
[An Empirical Study of Mamba-based Language Models](https://arxiv.org/abs/2406.07887),
Figure 7.*

Это эмпирический результат, а не гарантия. Если адресуемый слой встречается
слишком поздно, полезная деталь могла уже исчезнуть из промежуточного
представления. Если attention слишком мало, модели может не хватить глубины для
нескольких шагов retrieval и reasoning. Доля и расположение attention являются
параметрами задачи, а не универсальным рецептом.

## Где SSM особенно естественна

- непрерывные и очень длинные потоки;
- аудио и другие временные сигналы;
- геномные последовательности;
- инференс на устройствах с жёстким ограничением памяти;
- генерация с высокой пропускной способностью;
- задачи, где полезную историю действительно можно сжать в состояние.

В статье о Mamba рассматриваются разные типы данных именно потому, что
предположение о последовательной динамике применимо не только к тексту.

## Что в гибриде осталось от Transformer

Замена attention не означает отказ от всей архитектуры. Обычно сохраняются
токеновые embeddings, residual stream, RMSNorm, channel-mixing FFN или MoE,
каузальная next-token objective и decoder-only организация блоков. Меняется
прежде всего операция смешивания информации между позициями.

Поэтому полезнее говорить не «Transformer или не Transformer», а явно
перечислять:

```yaml
sequence_mixer:
attention_frequency:
recurrent_state_shape:
local_window:
ffn_or_moe:
cache:
  kv_per_attention_layer:
  state_per_recurrent_layer:
```

DeepSeek с MLA и sparse attention остаётся attention-based decoder Transformer:
он сжимает или разреживает архив, но не заменяет его рекуррентным состоянием.
Это важная контрлиния: давление на полное внимание создают одновременно SSM и
оптимизации внутри самого Transformer.

Мультимодальность — другая ось. Она отвечает за преобразование изображения,
аудио или видео в совместимые представления, а не за способ переноса памяти
вдоль последовательности. Подробный разбор находится в
[[02 Areas/ML & DL/00 Учебник/16 Multimodal Models/64 Мультимодальные модели]].

## Stateful serving: меняется не только kernel

У обычного Transformer состояние запроса — в основном append-only KV blocks.
У SSM, RWKV и linear attention состояние изменяется на каждом токене. У гибрида
движок ведёт оба типа cache. Это меняет несколько системных операций.

### Dynamic batching и preemption

Планировщик должен scatter/gather state для текущего набора последовательностей.
Если запрос вытеснен, недостаточно сохранить номер позиции: нужно сохранить
recurrent state всех соответствующих слоёв. При возвращении оно должно совпасть
с моделью, dtype, layout и точкой префикса.

### Prefix caching и ветвление

KV-cache естественно разбивается на страницы и может разделяться между общими
префиксами. Рекуррентное состояние соответствует всему префиксу целиком. Для
ветвления нужен snapshot ровно в точке общего начала. Слишком редкие snapshots
уменьшают cache hit, слишком частые расходуют память и bandwidth.

### Спекулятивное декодирование

После проверки draft-токенов часть продолжения может быть отклонена. KV-cache
можно обрезать после последнего принятого блока. Mutable state нужно вернуть в
предыдущее состояние: хранить копии speculative steps, применять copy-on-write
или переигрывать переход от checkpoint.

### Prefill/decode disaggregation

Для Transformer между pools передаётся KV-cache prompt. Для чистой SSM можно
передать финальное состояние prefill; для гибрида — recurrent state и KV редких
attention-слоёв. Объём может уменьшиться, но протокол должен понимать оба layout,
sharding и versioning.

### Точность состояния

Квантизация весов не гарантирует безопасную квантизацию состояния. Ошибка
recurrent update может накапливаться по длине потока, поэтому некоторые runtime
сохраняют SSM state в FP32 при низкоточных весах. Reference, parallel prefill и
single-token paths нужно проверять на численную эквивалентность.

Подробная механика находится в
[[02 Areas/ML & DL/00 Учебник/14 Inference и оптимизация/55b Scheduling — continuous batching, chunked prefill и prefix caching|главе о scheduling]],
[[02 Areas/ML & DL/00 Учебник/14 Inference и оптимизация/58 Спекулятивное декодирование|спекулятивном декодировании]] и
[[02 Areas/ML & DL/00 Учебник/14 Inference и оптимизация/58a2 Раздельное обслуживание prefill и decode|P/D disaggregation]].

## Как оценивать архитектуры честно

Для качества памяти нужны разные задачи:

- copying и phonebook — точное воспроизведение редкой детали;
- MQAR — несколько ассоциаций ключ–значение и несколько запросов;
- state tracking — последовательное изменение сущности;
- multi-hop retrieval — несколько связанных обращений к контексту;
- реальные длинные документы — шум и неодинаковая плотность фактов;
- language modeling — общее качество, но не замена retrieval-тестам.

Для эффективности отдельно измеряют prefill throughput и TTFT, decode TPOT по
длине истории, bytes состояния на sequence, concurrency, batch scaling,
reference/kernel parity, prefix reuse, branching, preemption и speculative
rollback. Модели на разных данных и training budget нельзя выдавать за чистый
архитектурный эксперимент.

## Что означает «готово к production»

Между статьёй и рабочим сервисом есть независимые ступени:

1. опубликована формула и controlled experiment;
2. доступны веса и reference implementation;
3. есть быстрые training, prefill и decode kernels;
4. mainstream runtime умеет загрузить класс модели;
5. проверены quantization, parallelism, batching, prefix cache и speculation;
6. есть воспроизводимый workload benchmark под SLO;
7. есть наблюдаемая эксплуатация с мониторингом и rollback.

Поддержка model class доказывает возможность выполнить forward в некоторой
конфигурации. Она не доказывает feature parity с Llama-подобным Transformer.
На 1 сентября 2026 года [официальный список vLLM](https://docs.vllm.ai/en/latest/models/supported_models/)
содержит отдельные классы для
Mamba, Mamba-2, Jamba, Falcon Mamba, Falcon-H1, Nemotron-H, Qwen3-Next и Kimi
Linear. Это подтверждает только наличие serving path: совместимость конкретного
checkpoint с quantization, prefix reuse, speculative decode, LoRA, tensor
parallelism и P/D disaggregation всё равно проверяется отдельно по нужной
версии движка и workload.

Актуальный срез Mamba, Jamba2, Falcon-H1, Nemotron, Qwen3-Next, Kimi Linear,
RWKV и TTT ведётся в
[[02 Areas/ML & DL/03 Исследовательские линии/Альтернативы полному attention и stateful sequence models|обзоре направления]],
потому что таблица runtime устаревает быстрее учебной механики.

## Где находится риск для Transformer

Transformer сохраняет преимущество там, где нужен произвольный точный доступ к
множеству редких деталей, сложное ветвление запросов и максимально зрелый
serving stack. Риск выше при очень длинном decode, непрерывных потоках, edge
устройствах и задачах, где история действительно хорошо сжимается в состояние.

Поэтому наиболее правдоподобный сдвиг — не внезапная победа одной Mamba, а
потеря монополии полного внимания. Большинство слоёв может обслуживаться дешёвым
состоянием, а точное адресуемое чтение становится редкой дорогой операцией.

## Ошибки, которые нужно видеть

> [!danger] «SSM помнит бесконечный контекст»
> Состояние имеет фиксированный размер. Модель может продолжать обрабатывать
> поток, но не способна без потерь хранить в нём неограниченное число деталей.

> [!danger] «Линейная сложность всегда означает более высокую скорость»
> На коротких последовательностях постоянные расходы и качество реализации ядер
> могут перевесить асимптотическое преимущество.

> [!danger] «Параллельное обучение и рекуррентный инференс — две разные модели»
> Это две эквивалентные или согласованные формы вычисления одного набора весов.

> [!danger] «Mamba — просто RNN»
> Рекуррентный инференс — только часть конструкции. Структурированная
> параметризация, зависимый от входа отбор и эффективное сканирование определяют
> практическое устройство модели.

> [!danger] «Гибрид устраняет KV-кэш»
> Кэш остаётся во всех слоях внимания.

> [!danger] «Linear attention — это Mamba под другим названием»
> Оба подхода используют компактное состояние, но выводят правило памяти из
> разных конструкций: SSM-динамики и ассоциативной matrix memory.

> [!danger] «TTT — это reasoning на большем числе токенов»
> TTT меняет fast weights или параметры модели при чтении контекста. Reasoning
> test-time compute выполняет больше шагов вывода с фиксированными весами.

> [!danger] «Поддерживается runtime — значит готово к production»
> Наличие model class не доказывает работу quantization, prefix reuse,
> speculation, P/D disaggregation и нужного workload под SLO.

## Практика

### 1. Развернуть рекуррентное обновление

Для scalar:

$$
h_t=0.9h_{t-1}+x_t,\qquad y_t=h_t
$$

вычислите первые четыре шага вручную и выпишите ядро свёртки:

$$
[1,\ 0.9,\ 0.9^2,\ 0.9^3].
$$

### 2. Добавить управляемую запись

Пусть вентиль $g_t\in[0,1]$ управляет обновлением:

$$
h_t=(1-g_t)h_{t-1}+g_tx_t.
$$

Для последовательности с маркером задайте $g_t$ вручную и покажите, как
состояние сохраняет только выбранный токен.

### 3. Сравнить расход памяти

Для Transformer из 32 слоёв с восемью KV-головами и гибрида, в котором внимание
используется в каждом четвёртом слое, вычислите размер кэша при контексте 128
тысяч токенов. Затем добавьте фиксированное состояние SSM.

### 4. Сопоставить иллюстрацию с кодом

Сопоставьте схему из статьи о Mamba с модулями официального репозитория:
`in_proj`, короткой свёрткой, селективным сканированием, вентилем и `out_proj`.
Сначала восстановите путь тензоров через блок и лишь затем переходите к CUDA-ядру.

## После главы нужно уметь

- восстановить причинную цепочку RNN → LSTM → Seq2Seq → attention → Transformer;
- объяснить, почему attention решает fixed-vector bottleneck, но создаёт растущий
  архив K/V;
- вывести свёрточную форму линейной SSM;
- объяснить, как вход управляет записью и чтением состояния;
- сравнить постоянную память внимания и рекуррентной модели;
- различать Mamba, linear attention, Gated DeltaNet, RWKV и RetNet;
- различать TTT-layer и TTT-E2E;
- объяснить параллельную, рекуррентную и блочно-рекуррентную формы;
- подобрать проверки для точного извлечения сведений;
- различать interleaved, scheduled и parallel hybrid;
- объяснить, почему MiniMax M2 является важным отрицательным свидетельством, а
  не доказательством бесполезности efficient attention;
- перечислить stateful serving contracts;
- отделять paper, weights, runtime support и production evidence.

## Материалы, на которых построена глава

### Основные статьи

- [Gu & Dao — Mamba](https://arxiv.org/abs/2312.00752).
- [Dao & Gu — Transformers are SSMs / Mamba-2](https://arxiv.org/abs/2405.21060).
- [Peng et al. — RWKV](https://arxiv.org/abs/2305.13048).
- [Peng et al. — RWKV-7 «Goose»](https://arxiv.org/abs/2503.14456).
- [Sun et al. — RetNet](https://arxiv.org/abs/2307.08621).
- [Lieber et al. — Jamba](https://arxiv.org/abs/2403.19887).
- [Lahoti et al. — Mamba-3](https://arxiv.org/abs/2603.15569).
- [Yang et al. — Gated Delta Networks](https://arxiv.org/abs/2412.06464).
- [Sun et al. — TTT layers](https://arxiv.org/abs/2407.04620).
- [Tandon et al. — End-to-End TTT](https://arxiv.org/abs/2512.23675).
- [Arora et al. — Zoology / MQAR](https://arxiv.org/abs/2312.04927).
- [Jelassi et al. — Repeat After Me](https://arxiv.org/abs/2402.01032).
- [Waleffe et al. — An Empirical Study of Mamba-based Language Models](https://arxiv.org/abs/2406.07887).
- [MiniMax — Why Did MiniMax M2 End Up as a Full Attention Model?](https://platform.minimax.io/docs/guides/text-m2-full-attention).

### Визуальные объяснения и код

- [Maarten Grootendorst — A Visual Guide to Mamba and State Space Models](https://www.maartengrootendorst.com/blog/mamba/)
  — более 50 последовательных иллюстраций от SSM к селективному сканированию.
- [Official Mamba repository](https://github.com/state-spaces/mamba).
- [CMU 10-423/623/723 — State Space Models and Hybrid Models](https://www.cs.cmu.edu/~mgormley/courses/10423/slides/lecture21-ssm.pdf).
- [Berkeley CS288 — Sequence Models](https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_05_Sequence_Models.pdf).
- [Berkeley CS288 — Sequence-to-Sequence Models](https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_06_Seq2seq.pdf).
- [Berkeley CS288 — Advanced Architectures](https://cal-cs288.github.io/sp26/assets/slides/CS288_sp26_14_Advanced_Architectures.pdf).
- [[02 Areas/ML & DL/Papers/Mamba]]
- [[02 Areas/ML & DL/Papers/RWKV]]
- [[02 Areas/ML & DL/Papers/RetNet]]
- [[02 Areas/ML & DL/Papers/Jamba SSM-Transformer Hybrid]]

**Дальше:** [[02 Areas/ML & DL/00 Учебник/11 Pre-training и Scaling/41 Сбор, очистка и смеси данных|как сырой корпус превращается в обучающую смесь.]]
