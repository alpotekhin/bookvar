---
title: Кластеризация и её ограничения
type: textbook-chapter
status: canonical
last_updated: 2026-07-24
primary_sources:
  - https://inria.github.io/scikit-learn-mooc/clustering/
  - https://scikit-learn.org/stable/modules/clustering.html
  - https://ml-visualized.com/
---

# Кластеризация и её ограничения

Clustering ищет структуру в $X$ без target labels. Результат алгоритма не
является автоматически «истинными классами»: cluster определяется objective,
metric и inductive assumptions конкретного метода.

## K-means

K-means ищет $K$ центров $\mu_1,\ldots,\mu_K$ и assignments $z_i$, минимизируя
within-cluster sum of squares:

$$
\min_{\mu,z}\sum_{i=1}^{n}
\lVert x_i-\mu_{z_i}\rVert_2^2.
$$

Lloyd algorithm повторяет два шага:

1. назначить каждый объект ближайшему центру;
2. заменить центр средним назначенных объектов.

Objective не возрастает, но решение зависит от initialization и может попасть
в локальный минимум. Поэтому используется несколько запусков `n_init`, после
чего выбирается решение с меньшей inertia.

![[Assets/Sources/Scikit-learn MOOC/figures/clustering_quiz_kmeans_scaled.svg|K-means scaled data]]
![[Assets/Sources/Scikit-learn MOOC/figures/clustering_quiz_kmeans_not_scaled.svg|K-means unscaled data]]

*Euclidean distance зависит от масштаба координат. Один и тот же dataset после
разного scaling приводит к разным partitions. Источник: scikit-learn MOOC,
CC BY 4.0.*

Полный исходный notebook:
[[05 Источники/Courses/Scikit-learn MOOC/python_scripts/clustering_kmeans.py|K-means clustering]].
Пошаговая визуализация:
[[05 Источники/Courses/Machine Learning Visualized/book/main.pdf|Machine Learning Visualized — K-means section]].

## Предположения k-means

K-means хорошо работает, когда clusters:

- приблизительно выпуклы и изотропны;
- сопоставимы по variance и размеру;
- разделимы в Euclidean metric;
- корректно представлены выбранными features.

Он плохо описывает кольца, вытянутые manifolds, clusters сильно разной плотности
и данные с многочисленными outliers. Увеличение $K$ всегда уменьшает inertia,
поэтому минимизировать её по $K$ бессмысленно.

## Как оценивать clustering

Если reference labels доступны только для анализа, применяют adjusted Rand
index или adjusted mutual information. Слово *adjusted* важно: метрика
корректируется на совпадение, возникающее случайно.

Без labels используют внутренние metrics. Silhouette для объекта $i$:

$$
s(i)=\frac{b(i)-a(i)}{\max(a(i),b(i))},
$$

где $a(i)$ — средняя дистанция до своего cluster, $b(i)$ — минимальная средняя
дистанция до другого cluster. Значение близкое к 1 указывает на отделённый
объект, около 0 — на границу, отрицательное — на возможное неверное assignment.

![[Assets/Sources/Scikit-learn MOOC/figures/iris-silhouette.svg|Silhouette diagram]]

*Silhouette сравнивает cohesion внутри cluster с separation от ближайшего
другого cluster. Источник: scikit-learn MOOC, CC BY 4.0.*

Внутренняя метрика наследует assumptions выбранной distance. Высокий
silhouette не доказывает прикладную полезность clusters.

## Stability

Практически важный вопрос: сохраняется ли partition при resampling, смене seed
и небольшом perturbation features? Если clusters исчезают при незначительном
изменении данных, интерпретировать их как устойчивые группы нельзя.

Проверка stability:

1. повторить clustering на bootstrap или subsamples;
2. сопоставить clusters между запусками;
3. измерить agreement assignments;
4. исследовать чувствительность к scaling, metric и hyperparameters.

## Density-based clustering

DBSCAN и HDBSCAN определяют clusters как плотные области, разделённые
разреженными участками. Они способны находить невыпуклые формы и помечать
часть объектов noise. В DBSCAN критичны radius `eps` и `min_samples`;
HDBSCAN строит иерархию плотности и извлекает устойчивые clusters, лучше
работая при неодинаковой локальной плотности.

Эти методы также не свободны от assumptions: distance concentration в высокой
размерности и неудачное feature representation могут сделать понятие
«плотности» бессодержательным.

Полные исходные материалы:

- [[05 Источники/Courses/Scikit-learn MOOC/python_scripts/clustering_hdbscan.py|HDBSCAN]];
- [[05 Источники/Courses/Scikit-learn MOOC/python_scripts/clustering_supervised_metrics.py|Clustering with supervised metrics]];
- [[05 Источники/Courses/Scikit-learn MOOC/python_scripts/clustering_transformer.py|Clustering as feature engineering]];
- [[05 Источники/Courses/Scikit-learn MOOC/jupyter-book/clustering/clustering_module_take_away.md.md|Module take-away]];
- [[05 Источники/Courses/Scikit-learn MOOC/python_scripts/clustering_sol_01.py|K-means exercise solution]];
- [[05 Источники/Courses/Scikit-learn MOOC/python_scripts/clustering_sol_02.py|Evaluation exercise solution]].

## Что нельзя заключать по двумерной картинке

PCA, t-SNE и UMAP меняют геометрию. Красивые острова на 2D projection не
доказывают существование separable clusters в исходном пространстве. Сначала
нужно сформулировать, какую структуру сохраняет projection, затем проверять
clusters в representation, соответствующем задаче. Dimensionality reduction
поэтому вынесено в следующую самостоятельную главу, а не смешано с k-means.

← [[05 Оценивание, кросс-валидация и выбор порога|Оценивание и cross-validation]] ·
[[06a Спектральная кластеризация и графовый Laplacian|Спектральная кластеризация]] →
