---
title: Производная и градиент
type: textbook-chapter
status: canonical
last_updated: 2026-07-20
primary_sources:
  - https://d2l.ai/chapter_preliminaries/calculus.html
  - https://cs231n.github.io/optimization-2/
---

# Производная и градиент

Обучение нейросети сводится к повторению одного вопроса: как изменить параметры,
чтобы ошибка стала меньше? Производная отвечает на него локально — для малых
изменений около текущей точки. Градиент собирает такие ответы для всех
параметров сразу.

## Производная измеряет локальную чувствительность

Пусть $y=f(x)$. Увеличим $x$ на небольшое число $h$ и посмотрим, насколько
изменится результат:

$$
\frac{f(x+h)-f(x)}{h}.
$$

При достаточно малом $h$ это отношение приближается к производной

$$
f'(x)=\lim_{h\to0}\frac{f(x+h)-f(x)}{h}.
$$

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/foundations-source-first/mml-secant-to-tangent.png]]

*Синяя секущая измеряет средний наклон на конечном отрезке. Когда вторая точка
приближается к первой, секущая переходит в касательную, а отношение
$\delta y/\delta x$ — в производную. Фрагмент рис. 5.3 из Marc Peter
Deisenroth, A. Aldo Faisal, Cheng Soon Ong,
[Mathematics for Machine Learning, p. 141](https://mml-book.github.io/book/mml-book.pdf).*

У производной есть одновременно геометрический и вычислительный смысл. На
графике функции это наклон касательной. В программе — коэффициент, который
приближённо переводит малое изменение входа в изменение выхода:

$$
f(x+\Delta x)\approx f(x)+f'(x)\Delta x.
$$

Для $f(x)=x^2$ производная равна $2x$. В точке $x=3$ она равна 6, поэтому
увеличение аргумента на $0{,}01$ должно увеличить функцию примерно на $0{,}06$:

$$
3{,}01^2-3^2=0{,}0601.
$$

Разница между $0{,}0601$ и линейным прогнозом $0{,}06$ возникает из-за кривизны
функции. Чем меньше шаг, тем точнее локальное линейное приближение.

## Знак и величина

Знак производной указывает, в какую сторону меняется функция.

| $f'(x)$ | Если немного увеличить $x$ | Чтобы локально уменьшить $f$ |
|---:|---|---|
| $>0$ | $f$ возрастёт | уменьшить $x$ |
| $<0$ | $f$ уменьшится | увеличить $x$ |
| $=0$ | линейного изменения нет | проверить кривизну и окрестность |

Большая абсолютная величина означает сильную локальную чувствительность, но не
гарантирует большого изменения на произвольном шаге. Производная описывает
окрестность точки; если шаг слишком велик, линейное приближение перестаёт быть
надёжным. Отсюда возникает роль learning rate в оптимизации.

## Частная производная

Функция потерь нейросети зависит не от одного числа, а от множества параметров:

$$
L=L(\theta_1,\theta_2,\ldots,\theta_n).
$$

Чтобы узнать чувствительность к одному параметру, остальные временно считают
постоянными. Так определяется частная производная
$\partial L/\partial\theta_i$.

Например,

$$
f(x,y)=x^2+3xy+y^2,
$$

$$
\frac{\partial f}{\partial x}=2x+3y,
\qquad
\frac{\partial f}{\partial y}=3x+2y.
$$

В точке $(x,y)=(1,2)$ получаем $(8,7)$. Первое число говорит, как изменится
$f$, если двигаться только по оси $x$; второе — если двигаться только по оси
$y$.

## Градиент

Все частные производные скалярной функции собираются в вектор:

$$
\nabla_{\boldsymbol\theta}L=
\begin{bmatrix}
\frac{\partial L}{\partial\theta_1}\\
\frac{\partial L}{\partial\theta_2}\\
\vdots\\
\frac{\partial L}{\partial\theta_n}
\end{bmatrix}.
$$

Градиент направлен в сторону наиболее быстрого локального роста функции.
Поэтому шаг в противоположную сторону уменьшает её в линейном приближении:

$$
\boldsymbol\theta_{t+1}
=\boldsymbol\theta_t-\eta\nabla_{\boldsymbol\theta}L,
$$

где $\eta$ — learning rate.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/foundations-source-first/google-gradient-descent.png]]

*Градиентный спуск повторяет один цикл: вычислить loss, определить направление
локального убывания и сделать шаг. Источник: Google,
[Machine Learning Crash Course — Gradient descent](https://developers.google.com/machine-learning/crash-course/linear-regression/gradient-descent),
[CC BY 4.0](https://creativecommons.org/licenses/by/4.0/).*

Это утверждение видно из многомерного линейного приближения:

$$
L(\boldsymbol\theta+\Delta\boldsymbol\theta)
\approx
L(\boldsymbol\theta)+
\nabla L^{\top}\Delta\boldsymbol\theta.
$$

Если выбрать $\Delta\boldsymbol\theta=-\eta\nabla L$, добавка станет
$-\eta\|\nabla L\|_2^2\le0$. Для достаточно малого шага loss должен
уменьшиться. Слово «достаточно» существенно: слишком большой шаг способен
перепрыгнуть через область уменьшения.

Когда вход и выход функции многомерны, производные сами образуют матрицы и
тензоры. Форма производной отвечает на практический вопрос: сколько выходных
координат изменяется при возмущении каждой входной координаты.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/foundations-source-first/mml-matrix-gradient-shape.png]]

*Первый способ строит три матрицы частных производных и складывает их вдоль
новой оси. Поэтому для $A\in\mathbb{R}^{4\times2}$ и
$x\in\mathbb{R}^{3}$ производная $dA/dx$ имеет форму
$4\times2\times3$: последняя ось перечисляет координаты $x$, а первые две
сохраняют форму результата $A$. Рис. 5.7a из
[Mathematics for Machine Learning, p. 155](https://mml-book.github.io/book/mml-book.pdf).*

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/foundations-source-first/mml-matrix-gradient-flatten.png]]

*Эквивалентный способ сначала разворачивает матрицу $A$ в вектор длины 8,
строит обычный Jacobian размера $8\times3$, а затем возвращает первым двум
осям форму $4\times2$. Рис. 5.7b из той же
[книги](https://mml-book.github.io/book/mml-book.pdf).*

## Правило сложной функции

Нейросеть — композиция функций. Если

$$
u=g(x),\qquad y=f(u),
$$

то влияние $x$ на $y$ проходит через промежуточное значение $u$:

$$
\frac{dy}{dx}=\frac{dy}{du}\frac{du}{dx}.
$$

Рассмотрим $u=x^2$ и $y=3u+1$. Тогда

$$
\frac{dy}{du}=3,
\qquad
\frac{du}{dx}=2x,
\qquad
\frac{dy}{dx}=6x.
$$

При $x=2$ малое изменение $x$ сначала меняет $u$ примерно в четыре раза
сильнее, а затем изменение $u$ усиливается ещё втрое. Общая чувствительность
равна произведению локальных чувствительностей: $12$.

В нейросети таких промежуточных величин тысячи. Вычислительный граф фиксирует,
какое значение из каких получено. Прямой проход вычисляет узлы слева направо;
обратный идёт от loss к параметрам и в каждом узле применяет правило сложной
функции.

![[02 Areas/ML & DL/00 Учебник/Assets/Figures/curated/foundations-source-first/mml-computation-graph-forward.png]]

*Многослойная сеть как композиция функций: параметры входят в отдельные узлы,
а итоговый loss зависит от всей цепочки. Фрагмент рис. 5.8 из
[Mathematics for Machine Learning, p. 160](https://mml-book.github.io/book/mml-book.pdf).*

## Почему в глубокой сети градиенты исчезают или растут

Для длинной композиции локальные производные многократно перемножаются:

$$
\frac{dL}{dx_0}=
\frac{dL}{dx_k}
\prod_{i=1}^{k}\frac{dx_i}{dx_{i-1}}.
$$

Если большинство множителей меньше единицы по модулю, произведение стремится к
нулю — ранние слои получают слабый сигнал. Если множители систематически больше
единицы, градиент может быстро вырасти. В последующих главах это объяснит
проблемы RNN, выбор инициализации, residual connections, normalization и
gradient clipping.

## Численная проверка производной

Аналитическую или автоматически вычисленную производную полезно проверять
центральной разностью:

$$
f'(x)\approx\frac{f(x+h)-f(x-h)}{2h}.
$$

Она обычно точнее односторонней разности при том же $h$. На практике нельзя
брать $h$ бесконечно малым: округление floating-point начнёт уничтожать разность
между близкими числами. Для небольших моделей gradient check сравнивает
численную оценку с backpropagation и помогает находить ошибки в производных.

```python
def numerical_grad(f, x, h=1e-5):
    return (f(x + h) - f(x - h)) / (2 * h)

f = lambda x: x**2
print(numerical_grad(f, 3.0))  # ≈ 6.0
```

Для миллиардов параметров так обучаться нельзя: каждый параметр потребовал бы
дополнительных forward passes. Численная производная служит тестом; эффективное
обучение использует reverse-mode automatic differentiation, которое в контексте
нейросетей называется backpropagation.

## Краткие итоги

- Производная — коэффициент локальной чувствительности, а не величина изменения
  на любом шаге.
- Частная производная меняет одну координату при фиксированных остальных;
  градиент собирает все такие производные.
- Отрицательный градиент задаёт направление локального убывания loss.
- Chain rule перемножает чувствительности вдоль композиции функций.
- Численная разность проверяет градиент, но не заменяет backpropagation.

## Источники и продолжение

- [[05 Источники/Courses/Machine Learning Visualized/book/main.pdf|Machine Learning Visualized — Complete Book]] — англоязычный вывод с геометрическими рисунками и реализацией градиентного спуска.
- [[05 Источники/Courses/Machine Learning Visualized/chapter1/interactive_linear_regression|Interactive Linear Regression]] — интерактивная траектория оптимизации линейной модели.
- Aston Zhang et al., [Dive into Deep Learning: Calculus](https://d2l.ai/chapter_preliminaries/calculus.html) — предел, частные производные, градиент и chain rule.
- Stanford CS231n, [Backpropagation, Intuitions](https://cs231n.github.io/optimization-2/) — производная как чувствительность и staged computation.
- Terence Parr, Jeremy Howard, [The Matrix Calculus You Need For Deep Learning](https://explained.ai/matrix-calculus/) — переход от скалярных примеров к матричным формулам.

**Дальше:** вычислительный граф и backpropagation покажут, как получить
производные loss по всем параметрам за один обратный проход.
